comp = ["-g"]

begin library "bulletml"
  files = ["syntax.ml" "printer.ml" "parser.ml"]
  requires = ["mparser" "xml-light"]
end

begin program "bulletml_tests"
  files = ["tests.ml"]
  requires = ["bulletml" "alcotest"]
end

begin program "bulletml_run"
  files = ["interp.ml"]
  requires = ["bulletml" "sdl" "sdl.sdlimage"]
end
